# Create an External Load Balancer
apiVersion: v1
kind: Service
metadata:
  name: show-service-external-load-balancer
  namespace: mad
spec:
  selector:
    app: show
  ports:
    - port: 10001
      targetPort: 8080  
  type: LoadBalancer